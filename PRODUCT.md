# Product

## Register

product

## Users

**主要使用者是开发者本人，外加少量同事与朋友。**

- **专业程度**：主使用者熟悉全部领域概念（clean plate、mask、阶段 DAG、accept 门），不需要教学；偶发使用者需要靠界面自解释「现在停在哪一步、为什么停、下一步该点什么」，但不需要完整新手引导。
- **物理场景**：白天、光照正常的房间，短时间多次进入 —— 打开一叠图、跑一轮、逐页校完文字、导出、关掉。不是一坐两小时的长会话工具。**因此默认亮色主题**；暗色不在首期范围。
- **要完成的工作**：把一叠 16:9 页面图片，在保留视觉效果的前提下，变成 PowerPoint 里真正可编辑的原生文本。
- **任一屏幕上的主任务**：控制台是「哪页卡住了、卡在哪一步」；文本复核页是「这块字对不对、该归哪类」；最终确认页是「底板干不干净、能不能签字」。

## Product Purpose

PPT Maker 是本地优先的图片转可编辑 PPTX 工具。转换引擎已经稳定（M3/M4 已交付），客户端的价值不在于「能不能做到」，而在于**把人工确认这件事的成本压到最低**。

全链路只有两个人工停顿点 —— 文本复核门与最终产物确认 —— 客户端存在的全部理由就是让这两个停顿又快又不出错。

成功的样子：一叠页面从导入到导出，人只需要看该看的、改该改的，不需要猜界面在说什么，也不需要怀疑界面说的是不是真的。

## Brand Personality

**校样台**（proof desk）—— 印前打样与编辑部校样纸的隐喻。这个产品干的本来就是校对：两个来源的文字逐块比对，标出差异，人工签字确认。视觉系统直接借用这套已经存在了一百年的、所有人都读得懂的传统，而不是再发明一套通用警告色。

三个词：**克制、精确、可信**。

- **克制**：颜色是稀缺资源。界面本身近乎无彩，因为预览区旁边任何带彩的界面色都会污染「这张底板到底干不干净」的判断。
- **精确**：这是一个满屏计数与计时的应用（`7/9`、`已用 42s`、`待确认 3`）。这些数字的呈现方式是产品气质的一部分，不是附属信息。
- **可信**：界面说完成，磁盘上就必须真的完成。

记忆点来自**少而准**，不来自多而艳：一抓校对红用在差异与待确认上，全屏唯一一个主行动，此外几乎不用彩色。

### 硬约束：不许滑向奶油色

「纸」的隐喻有一个众所周知的失败模式 —— 翻译成暖调近白底（cream / sand / paper / parchment / linen / ivory，OKLCH L 0.84–0.97、C < 0.06、hue 40–100 这个色带）。**那是 2026 年 AI 生成界面的饱和默认，本项目明令禁止**，连 `--paper` `--cream` `--sand` 这类令牌命名本身都算破绽。

「纸」必须是 **chroma 0 的真中性白**（或极微量偏向品牌自身色相、而非默认偏暖）。暖意由校对红、字体和排版承担，绝不由底色承担。

## Anti-references

四条全部为硬性反参考，后续每个设计决策都要拿它们卡：

1. **当前这套从 Airtable 营销站扒来的皮**。`DESIGN.md` front-matter 至今写着 `Airtable-design-analysis`。营销站的品牌系统（signature-coral / signature-forest 这类「品牌电压」签名色）被硬套到工具型应用上，签名色被拿去当状态语义色用（coral=失败、mustard=stale）。这是 register 错配，是当前「怎么调都不对劲」的根因。
2. **SaaS 仪表盘模板**。大数字 + 小标签的 KPI 卡片排、渐变强调、等大小卡片无限重复。
3. **毛玻璃 / 渐变字 / 彩色左边条**。装饰性 backdrop-filter、`background-clip: text` 渐变标题、卡片左侧 >1px 的彩色粗边条。
4. **企业后台 / Ant Design 默认皮**。蓝色主色 + 密密麻麻的表单控件 + 千篇一律的后台气质。能用，但没有任何人会记住它。

## Design Principles

1. **图片是主角，界面退让。** 预览、缩略图、合成对比区是用户真正在看的东西。这些区域周边的界面必须近乎无彩、不抢注意力，否则用户没法判断底板颜色对不对、文字残没残留。任何「让界面更好看」的用色，一旦落在预览区旁边，都要先过这一关。

2. **不确定必须长得像不确定。** M4 全链路走查暴露的六个缺陷里有四个同属一类：界面给了成功反馈而实际什么都没发生，或写下的记录与事实相反。这类静默分歧比报错危险，因为它看起来是成功的。设计层面意味着：加载中、失效、未知、待确认各自要有明确且互不相同的表达，绝不允许默认渲染成完成态。

3. **「差异 / 待确认 / 已一致」是产品的核心词汇。** 视觉系统首先服务这三类，其次才服务通用组件。校对红标差异、中性标已一致 —— 这套语义优先于任何美学偏好。

4. **键盘是一等输入，不是补充。** M4 已交付「全程仅用键盘即可复核完一页」的能力，但当前实现里 `focus-visible` 出现 0 次 —— 能力与实现直接矛盾。焦点可见性、快捷键提示、Tab 顺序与视觉顺序一致，都属于功能而非润色。

5. **状态不靠颜色单独承载。** 完成 / 待人工 / 失效 / 失败四种状态必须同时携带形状、图标或文字，色弱用户也能分辨。这同时也是原则 3 的实现约束。

## Accessibility & Inclusion

目标档位 **WCAG 2.2 AA**，四条为硬性验收项：

- **键盘焦点全程可见。** 每个可交互元素都要有 `:focus-visible` 表达，且焦点环自身对比度 ≥ 3:1。当前实测 24 个组件中 `focus-visible` 命中 0 次，是必须清零的缺口。
- **正文对比度 ≥ 4.5:1，大字（≥18px 或粗体 ≥14px）≥ 3:1。** 占位符文字同样按 4.5:1 要求，不适用「灰一点更优雅」的例外。重做令牌时逐个验算，不靠肉眼判断。
- **状态不能只靠颜色区分。** 见 Design Principle 5。
- **尊重 `prefers-reduced-motion`。** 每个动效都要有减弱分支（通常降为交叉淡入或直切）。当前 38 处 `transition` 全部没有兼容分支。

其他：中文为界面唯一语言，暂不做 i18n；不承诺屏幕阅读器完整适配，但语义标签与 `aria-*` 该给的要给（当前已有 25 处 `aria-`，保持并补齐）。
