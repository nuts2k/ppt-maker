# PPT Maker 路线图

> 状态：执行中（M0–M4 已完成；进行中 M5 页面来源；其后 M6 内容策划工作台、M7 可靠性与本地交付）
>
> 目标用户：开发者本人
>
> 首期平台：macOS + Microsoft PowerPoint for Mac
>
> 首期产品：已有 16:9 页面图片 → 文字原生可编辑的 PPTX

## 1. 产品目标

PPT Maker 的核心价值是在保留 AI 生成页面视觉效果的同时，把独立版式文字恢复为 PowerPoint 原生可编辑文本。

首版每页采用：

```text
完整 clean plate 背景图
+
微软雅黑原生文本框
```

容器、图标、箭头、图表、插画、阴影和渐变保留在背景图中，不要求原生化。标题、正文、图注、解释性标签、列表和标注文字必须进入文本层；与视觉对象融为一体的数字、缩写和符号必须保留为对象细节。

最终经过人工复核后，目标文字的内容正确率、覆盖率和可编辑率必须达到 100%。文字特效可以后期人工补齐，不以位图降级代替目标文字。

## 2. 已确认的产品约束

- 服务开发者本人长期本地使用，暂不面向外部用户发布。
- 首个可用产品聚焦图片转 PPTX，内容策划和图片生成后续接入。
- 输入和输出统一为 16:9；非 16:9 输入默认拒绝。
- 首期支持简体中文、英文和中英混排。
- 所有原生文本统一使用微软雅黑；缺失时默认阻止导出，可显式配置备用字体。
- OCR 离线优先；低置信度结果默认不自动上传。
- 云端 OCR/视觉增强由开发者显式触发，可按项目配置受信任 Provider 自动回退。
- 图像编辑和复杂视觉理解允许调用外部 API，项目数据和中间产物保存在本机。
- 原始文案仅作识别参考，图片中实际可见的独立版式文字才是恢复对象。
- 必须支持人工复核、结构化校正和受影响阶段增量重跑。

## 3. 总体质量原则

分别评测以下维度，不能用单一“视觉相似”分数代替：

- 目标文字发现、内容、分块、换行、旋转和位置。
- 独立版式文字与对象内符号的分类。
- clean plate 的文字残留、容器完整性和非文字区域误改。
- PowerPoint for Mac 中的文本框排版和最终可编辑性。
- 每页人工复核与校正耗时；只记录趋势，不设硬性准入门槛。

## 4. 里程碑

### M0 项目骨架与技术基线

状态：已完成（2026-07-20）

目标：建立最小、可重复的工程环境，并用小型实验确定首期技术基线。

进入条件：本路线图通过评审。

主要交付物：

- 项目目录、包管理、Node/TypeScript 和质量命令。
- macOS 离线 OCR、图片处理、PptxGenJS 与 PowerPoint for Mac 的技术验证。
- 16:9 坐标、页面工作区、文字块和阶段产物契约。
- 基于首批真实代码形成的 Trellis 工程规范。

完成条件：开发环境可重复建立；最小命令、测试、类型检查和格式检查可运行；关键技术选择有实验依据。

非目标：完整 OCR 流水线、Electron UI、多页项目管理。

计划 Trellis 子任务：`project-foundation`

### M1 单页图片转可编辑 PPTX 原型

状态：已完成（2026-07-21）

目标：跑通单张图片从识别、人工校正、去字到 PPTX 的完整链路。

进入条件：M0 完成核心数据契约和技术基线。

已交付（合成 fixture 自动化全绿）：

- 单页工作区 + 阶段 DAG + 输入指纹复用/失效；显式阶段命令与 `slide run --from`（上传门/人工门停止）。
- 16:9 校验、微软雅黑预检、Apple Vision 离线 OCR（含字符/子串 `glyphHints`）。
- 显式云端视觉分析（Responses API `gpt-5.6-sol`），候选合并生成可编辑 `text-blocks.json`，`validate-review` 结构化校验。
- 自动字形 mask（sharp I/O + TS 像素算法：颜色/边缘/连通域/膨胀/多边形/glyphHints 软先验）与覆盖率统计。
- gpt-image-2 clean plate（固定**请求** 2048x1152/high/PNG，实返尺寸见 M2 技术结论）+ 离线检查（尺寸/文字残留/mask 外差异/容器环）+ 人工 accept 门。
- 微软雅黑原生文本框 PPTX + ZIP/XML/16:9/文字/字体/形状自动检查 + PowerPoint for Mac 人工 accept 门。
- 分阶段报告（自动检查与人工接受分区，未完成不为成功）；合成 fixture 五类元素端到端覆盖。

技术结论：

- Apple Vision 可离线输出中英文与字符四边形；`glyphHints` 仅作 mask 局部先验，不是精确字形轮廓。
- sharp（预编译平台包，无 install 脚本，免白名单）负责编解码/裁剪/合成，像素算法用 TS 自实现，字节级确定、可建像素基线。
- 指纹投影（design §6）：mask/clean 只对几何/分类/mask 参数投影哈希，内容/样式变更只重跑 PPTX。
- gpt-image-2 类型契约在 openai@6.48.0 完整可用；`size` 经 `(string & {})` 软收口，固定常量 + 像素尺寸断言补偿；mask 为模型引导非硬约束，故辅以离线残留/容器检查 + 人工门。

进入 M2 条件：✅ 已满足 — 开发者实际生成的复杂真实页完成完整本地工作区流程，人工确认全部目标文字进入原生层、clean plate 无残留、容器与对象内符号未破坏，并在 PowerPoint for Mac 确认 16:9/可打开/可编辑/微软雅黑。

非目标：多页批处理、桌面 UI、内容生成、非文字元素矢量化。

Trellis 子任务：`07-20-single-slide-editable-pptx`

### M2 评测体系与路线决策

状态：已完成（2026-07-22）

目标：用真实样例量化核心链路，决定是否以及如何进入长期工具建设。

进入条件：✅ 已满足 — M1 能保存并重放全部中间产物。

已交付：

- 25 页 PPSO 投标方案评测集（gpt-image-2 生成，覆盖深色/浅色背景、密集/稀疏文字、蓝图/架构图/时间线/表格等 6 类页面）。
- 25 页全部走通 M1 pipeline（OCR→assist-review→mask→clean plate→PPTX），PPTX 自动检查通过率 84%（4 个失败为检查逻辑 false negative，已修复）。
- 失败模式目录：3 个 bug（OCR 负坐标、includeInMask 逻辑、XML 转义匹配），全部已修复。
- 路线决策：进入 M3，核心技术路线可行，问题在工程细节而非技术方向。

技术结论：

- gpt-image-2 生成接口可直出 16:9 比例，但**不接受精确像素指定**：请求 2048x1152 被静默
  改写为 1672x941（高度在 940/941 间浮动），比例误差 0.06%，在 0.5% 容差内。
  由此得出的落地约束——生成资产尺寸必须落盘后实测、不得用请求参数填充——见 M5 期间条目。
- 25 页批量处理验证了 pipeline 的稳定性和可重复性。
- assist-review 的 GPT-5.6-Luna 分类在中文商务 PPT 场景下基本可用，但 risk 块的 mask 默认行为需要修正（已修复）。

进入 M3 条件：✅ 已满足 — 端到端通过率 ≥ 80%，失败模式可归类且有改进方向。

非目标：通过目标文字位图降级提高表面成功率；提前开发完整桌面应用。

Trellis 子任务：`07-21-evaluation-suite`

### M3 多页本地转换工具

状态：已完成（2026-07-22）

目标：形成开发者本人可以长期使用的多页图片转 PPTX 工具。

进入条件：✅ 已满足 — M2 证明核心路线值得继续投入。

已交付：

- DeckManifest 契约（轻量索引 + N 个独立 slide workspace，slide 内部结构不变）。
- `deck init --images <dir>`：扫描目录下 PNG/JPEG，按文件名排序批量创建 slide workspace。
- `deck run --confirm-api --confirm-upload`：逐页串行执行 pipeline，自动通过 API/上传门，人工门逐页停止，结束后汇报每页状态。
- `deck status`：每页阶段状态 + 汇总统计，支持 `--json`。
- `deck export -o <path>`：多页合并为单一 PPTX，已验收页使用原生文本层，未完成页用源图占位（标记"待完成"），`--strict` 要求全部验收。
- `deck add-slide / remove-slide`：追加页面 / 软删除（不删磁盘数据）。
- `slide run --from` 扩展：新增 `--confirm-api / --confirm-upload` 自动通过门。
- 阶段恢复：中断后重启 `deck run` 可恢复，已完成阶段通过指纹复用跳过。

技术结论：

- Deck 层作为 slide workspace 的编排层，不修改 slide 内部结构，slide 命令全部复用。
- 串行执行 + 错误隔离在 25 页规模下可接受，并行优化留到有需求时再做。
- PptxGenJS 多页合成为 addSlide 循环调用，与单页逻辑一致。

进入 M4 条件：✅ 已满足 — deck init/run/status/export/add-slide/remove-slide 全部可用，typecheck 通过，139 测试全绿，端到端冒烟测试通过。

非目标：完整桌面可视化编辑、内容策划和图片生成、页面并行执行、参考文案批量匹配。

Trellis 子任务：`07-22-local-deck-workflow`

### M4 桌面复核工作台

状态：已完成（2026-07-27）

目标：把结构化文件校正升级为高效的可视化校正体验。

进入条件：✅ 已满足 — M3 的领域模型、阶段状态和资产契约稳定。

已交付：

- **批量优先控制台**（V2 重构，第一版体验被判定不合格后推倒重做）：Deck 总览的每页阶段轨道 + 实时计时、「一键处理全部」由 main 进程逐页串行执行、需人工介入的页自动归入待办队列、活动日志按 deck 追加写入 Electron userData（jsonl，跨重启保留）、manifest 中持久化的错误在界面可见、导出前置状态汇总与 `--strict` 开关。
- **链路收敛为双人工点**：全链路只剩「文本复核门」与「最终产物确认」两个停顿。`accept-clean` 不再单独停，滑块对比降级为最终确认页内的一档视图；不满意时在同一界面选「重做底板」（重跑 clean→pptx）或「回到文本复核」。
  （口径变更：M5 起为**最多三个人工点**，新增的「源图确认」按来源条件性激活——
  `generated` 页必停，`imported` / `extracted` 页自动放行。见 M5 D6。M4 交付内容本身不变。）
- **文本复核界面重构为列表主导的双源校对**：OCR 与 AI 文本逐块 diff，分「文字待确认 / 分类待确认 / 已一致」三区并直接显示计数，全程仅用键盘即可复核完一页，编辑溯源如实写入 `updatedAt` / `manual`。
- **最终确认页**：clean plate + 文本层的合成预览、pptx 六项自动检查与 clean 四组裸指标同屏可见，一次确认同时记录 `accept-clean` 与 `accept-pptx`。
- **质量护栏进 core**：`LAYOUT_TEXT_MUST_BE_MASKED`（error）堵死导出重影，`buildFreshBlock` 对 `layout_text` 默认 `includeInMask = true`。

技术结论：

- **V2 立的「不改造 `packages/core` 与 `apps/cli` 内部」约束已显式解除**（V2 的 D3 → 链路简化任务的 D8）。导出重影这类判据只能落在 core 的文档校验里；留在桌面端等于每个消费方各写一份，且 CLI 路径完全不设防。
- **「补齐 `maskParams` 调参 UI + 底板局部重绘」这条路已放弃**（链路简化 D1）：人工门的价值在于确认「文字对不对」，不在于让人调参数；底板不满意就整块重跑。
- **界面正反馈必须以磁盘状态为准**。全链路走查暴露的六个缺陷里有四个同属一类——界面给了成功反馈而实际什么都没发生，或写下的记录与事实相反（瞬态阶段失效是静默空操作、会话层状态盖住耐久层、验收写出与自动检查矛盾的人工清单、分区计数只增不减）。这类静默分歧比报错危险，因为它看起来是成功的。
- **既有工作区无需数据迁移**：`schemaVersion` / `workspaceVersion` 均未变，M3 时代产出的 deck 可直接被新版打开并严格导出。

已知缺口：✅ 三条已全部解决（2026-07-30）。

- ~~桌面端保存复核**不失效下游**，在已完成的页上改字并保存后产物不更新，界面却仍显示完成。~~
  → `07-28-review-ux-convergence` R5，改由 `main/save-invalidation.ts` 的 `decideInvalidation` 按粒度判定。
- ~~打开 deck 后没有切换工作区的入口，换 deck 只能重启应用。~~
  → `07-29-desktop-workspace-switch`，顶栏当前工作区块改为可点下拉。
- ~~`apps/cli/src/report/run.ts` 取 `clean_record` / `pptx_check` 时按 role 取第一条，会混入旧 attempt。~~
  → `07-28-review-ux-convergence` R6，改按 `lastSuccessfulAttemptId` + role 双条件。

完成条件：✅ 已满足 — 核心 CLI 能力均可从 UI 调用；界面不绕过复核、版本和恢复契约；全新 deck 端到端走查七项断言全过，既有工作区可打开并导出为全原生 PPTX。

非目标：账号、云同步、多人协作和普通用户级新手引导。

Trellis 子任务：`07-22-desktop-review-workbench`、`07-26-review-flow-simplification`、
`07-28-review-ux-convergence`（体验收敛）、`07-29-desktop-workspace-switch`（切换工作区）

### M5 页面来源

状态：进行中（2026-07-31 起）

> 原名「页面来源与内容策划」。规划阶段按 D3 把交互式内容策划拆为独立的 M6，
> M5 只保留三种来源的接入与生成执行侧。Trellis 父任务目录名沿用旧名
> `07-31-page-sources-and-content-generation`，不改名以免断掉引用。

目标：在稳定转换引擎上接入上游，让页面素材可以从多个入口进入同一条转换链路。

进入条件：✅ 已满足 — M3/M4 已能可靠处理生成图片并完成人工复核（2026-07-27，M4 端到端走查通过）。

#### 骨架：页面来源抽象，而非单向生成管线

M0–M4 的唯一入口是「一叠现成图片」（`deck init --images`）。M5 不是在它前面接一条
「策划 → 生成」的瀑布，而是把「这一页的图从哪来」抽象为独立维度：

| 来源 | 说明 | 现状 |
|---|---|---|
| `imported` | 已有图片文件，单张或整目录 | 已实现（M3 `deck init --images`、桌面端「从图片目录创建」） |
| `extracted` | 从 PDF 等文档逐页抽取 | 缺口 |
| `generated` | 由内容规格与视觉意图驱动模型生成 | 缺口 |

关键约束：**来源按页记录，同一个 deck 内可混合**。第 1/3/7 页用现成图、第 2 页从 PDF 抽、
第 4–6 页由模型生成，必须是常态而非特例。生成只是三种来源之一，不是主干。

由此，「图片生成失败或变更不得破坏已确认的下游产物」这条约束应实现为通用的
**换源操作**：替换某页源图后按现有指纹机制失效下游，与新图是重新生成的、换了个文件、
还是从 PDF 重抽的无关。若只针对「重新生成」做失效处理，「换一张图进去」这个更常见的
操作会失去保护。

现有契约缺口：`DeckSlideEntry` 有 `sourceImageName` 但无来源维度；deck 层只有
`add-slide` / `remove-slide`，没有「替换某页源图」的动作。

#### 主要交付物（四项，与子任务一一对应）

1. **页面来源契约与换源操作**：按页记录来源类型与溯源信息；新增源图确认闸门
   `accept-source`（`ArtifactAcceptance` 的第三个实例）；`replace-source` 接入既有指纹失效链路；
   加载期归一化保证既有工作区零迁移；CLI 入口与桌面端换源 UI。
2. **PDF 抽取（`extracted`）**：逐页渲染为位图并建立 slide workspace，按页探测可提取文本层、
   按页判定 16:9，不合格页进报告并跳过。
3. **图片生成（`generated`）**：内容规格双层契约、图片 Provider 抽象、提示词与模型版本、
   成本记录、规格文字落 `reference_text`、带调整说明重生成与规格回写、规格漂移检测。
4. **桌面端新建来源入口收口**：统一的「新建 deck 时选来源」界面、总览的来源列与漂移标注、
   批量源图确认界面（逐张接受 / 带说明重新生成）。

**M5 的 `generated` 只含生成执行侧**——从既有的内容规格文件出发产出图片。
从构思到规格的交互式策划过程属于 M6，两者以内容规格文件为契约衔接。
M5 提供一次性的「构思/大纲 → 规格初稿」动作，避免规格全靠手写，但那不是对话式策划。

#### 已定决策

原「待决策」两条已在 2026-07-31 的规划中定论：

- **矢量 PDF 一律位图化**（D1）。但 init 时探测并记录每页是否含可提取文本层，界面显式提示。
  理由：直取文本是另一条完整链路（版式还原、字体映射、坐标体系都不同），
  与 M0–M4 的位图链路无法共用；先记录探测结果，等真实需求出现再决定是否单开路径。
- **16:9 维持拒绝，尺寸假设全链路不动**（D2）。改为**逐页判定**：不合格页带尺寸与原因进入
  报告并跳过，不让单页异常挂掉整份导入。理由：放宽会击穿 deck 与 slide 两层的
  `aspectRatio: z.literal("16:9")` 以及几何、mask、导出的全部尺寸假设，代价远大于收益。

另有五条决策记于父任务 `prd.md`「已定决策」表（D3 策划独立成里程碑、D4 换源默认清空 `text_review`、
D5 子任务切分、D6 源图确认闸门、D7 规格双层结构）。

#### 完成条件

三种来源均可建立 deck 并跑通到可编辑 PPTX；同一 deck 内混合来源可用；
任一页换源后下游失效正确，已确认的其它页不受影响；
`generated` 页在进入 OCR 前必经人工确认，`imported` / `extracted` 自动放行且不伪造人工验收记录；
M3/M4 时代的既有工作区无需迁移即可打开、继续处理并严格导出。

非目标：交互式内容策划（归 M6）、矢量 PDF 直取文本、放宽 16:9、
通用 Agent CLI、MCP、插件市场和多种设计产物平台。

Trellis 父任务：`07-31-page-sources-and-content-generation`

子任务（实际目录名）：
`07-31-page-source-contract`（已归档）、`08-01-pdf-page-extraction`（已归档）、
`08-01-spec-driven-generation`（已归档）、`08-01-desktop-source-entry`（已归档）

### M6 内容策划工作台

状态：待开始

目标：把「一句构思」变成一份可执行的内容规格，让 `generated` 来源不必手写规格。

进入条件：M5 的内容规格契约已定稿并有真实使用（`generated` 页跑通到 PPTX）。

#### 与 M5 的分工

M5 是**执行侧**：规格 → 图 → slide。M6 是**策划侧**：构思 → 规格。
两者以**内容规格文件**为唯一契约衔接——M6 产出的规格，M5 的生成链路原样消费，
不需要为 M6 改动生成侧的任何契约。这条边界是 M5 规划阶段 D3 的核心结论：
策划是对话式、发散、反复推翻的，与转换引擎的确定性阶段图气质完全不同，混在一个里程碑里会两头都做不好。

主要交付物：

- 输入通道：构思一句话、背景材料、本地文件（既有 PPT / 文档 / 笔记）。
- 交互式提问：模型就受众、场景、篇幅、结构、风格逐项发问并收敛，而非一次性猜完。
- 对话式调整：整份大纲与逐页内容都可以用自然语言改，改动落到规格文件的对应条目。
- 规格文件的编辑与版本：与 M5 的 deck 级可编辑意图层对接，条目级可追溯。

完成条件：从一句构思出发，不手写规格文件即可产出一份可直接驱动 M5 生成链路的内容规格，
且逐页内容可对话式调整；调整落盘为规格条目变更，M5 侧如实反映漂移。

非目标：自动配图审美评判、模板市场、多人协同编辑规格。

计划 Trellis 子任务：`content-planning-workbench`

### M7 可靠性与本地交付

状态：待开始

> 原为 M6，因新立「M6 内容策划工作台」而顺延，内容不变。

目标：提高开发者本人长期使用时的稳定性和可维护性。

进入条件：核心工作流已经稳定产生实际价值。

主要交付物：异常恢复、备份、日志诊断、依赖预检、macOS 打包、数据迁移和 Windows 兼容性调研。

完成条件：常见失败可诊断、项目可备份恢复、升级不破坏既有项目；是否扩展到 Windows 或外部用户有明确结论。

非目标：默认承诺 Windows/Linux 支持或商业化发布。

计划 Trellis 子任务：`local-product-hardening`

## 5. 关键决策点

### M1 后

- 若目标文字无法可靠分类或 clean plate 大量破坏容器，暂停 UI 投入，调整识别、mask 或图像编辑路线。
- 不允许通过把目标文字保留在背景图中宣称完成。

### M2 后

- 根据评测结果决定继续 M3、增加技术实验，或收紧明确支持的页面类型。
- 人工校正耗时用于排序自动化改进，不单独决定项目是否继续。

### M3/M4 后

- 只有图片转 PPTX 工具已经稳定可用，才接入内容策划和图片生成。✅ 已满足，M5 起接入。
- 面向外部用户、Windows 正式支持和商业化能力必须单独立项。

### M5 期间

- **生成图直出 16:9 已实证通过**（2026-08-01，RK1）。✅ 回滚点解除，四种来源范围不变。
  实测 1672x941、比例误差 0.06%，证据与判读见
  `.trellis/tasks/08-01-spec-driven-generation/research/rk1/CONCLUSION.md`。
  实证走的是 `.env` 配置的第三方网关，**换官方端点或换 Provider 须重验**。
- **生成资产的尺寸必须落盘后实测，禁止用请求参数或常量填充**（RK1 衍生约束）。
  网关不保证返回请求尺寸，且高度在两次观测间浮动过。
  仓库已有反例：`clean/run.ts:329` 用常量硬填 `clean_plate` 资产尺寸，
  manifest 记 2048x1152 而磁盘实为 1672x941。这与「多代资产选取契约」同类——
  `assertWorkspaceAssetIntegrity` 只校验 sha256，抓不到元数据与文件不符。
- **来源契约由 M5 父任务独占**。四个子任务任一方发现契约需要改动，回父任务改，
  不在各自实现里微调——否则混合来源要到最后集成才炸。
- **若 `accept-source` 的加载期归一化被证明不可行**，须重新考虑该闸门的落地形式，
  而不是接受「既有工作区需要迁移」。零迁移是 M3/M4 已对使用者作出的承诺。

### M5 后

- 内容规格契约定稿即为跨里程碑契约，M6 只在其上做策划侧，不反过来改生成侧契约。
- 若 `generated` 来源的实际使用频率远低于预期，M6 应重新评估优先级，
  而不是默认按里程碑顺序开工。

## 6. Trellis 使用方式

- 本路线图对应父任务：`07-20-ppt-maker-mvp-roadmap`。
- 每个里程碑由独立子任务负责规划、实现、检查和归档。
- 后续子任务在前一里程碑接近完成时创建，避免一次性建立大量过时计划。
- 完成子任务后先更新本文件状态和关键决策，再提交并归档任务。
- 父任务在主要路线图工作完成前保持可追踪，不提前归档。

## 7. 明确不采用的首期方向

- 修改或直接继承 `open-design/`。
- Next.js sidecar、通用 Agent CLI runtime、MCP 和插件市场。
- HTML Deck 作为核心中间格式。
- 通用 `dom-to-pptx` 作为首版导出引擎。
- 任意比例、多字体识别、非文字元素矢量化和完全离线模型体系。
- 账号、云同步、多人协作、自动更新和商业化能力。
